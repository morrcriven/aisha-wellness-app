# Used by `vite build` (i.e. the GitHub Actions deploy).
# This is a public URL that the browser hits — safe to commit.
# The actual OPENAI_API_KEY lives only as a Cloudflare Worker secret.
VITE_OPENAI_PROXY_URL=https://aisha-wellness-proxy.aishamitha.workers.dev
